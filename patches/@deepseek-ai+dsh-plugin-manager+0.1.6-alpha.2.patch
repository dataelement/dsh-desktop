diff --git a/node_modules/@deepseek-ai/dsh-plugin-manager/lib/index.js b/node_modules/@deepseek-ai/dsh-plugin-manager/lib/index.js
index f93c99f..ae5d32f 100644
--- a/node_modules/@deepseek-ai/dsh-plugin-manager/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-plugin-manager/lib/index.js
@@ -896,7 +896,13 @@ let PluginManager = (() => {
 				});
 			};
 			const result = this.change(async (result) => {
-				if (spec.trim() === "" || spec.startsWith("-")) throw new ManagementFailure("invalid-spec");
+				let parsed;
+				try {
+					parsed = parseInstallSpec(spec);
+				} catch (error) {
+					if (error instanceof InvalidInstallSpecError) throw new ManagementFailure("invalid-spec", error.reason);
+					throw error;
+				}
 				if (stopped()) throw new InstallCancelledError();
 				if (options?.approvedBuilds !== void 0) {
 					await approveBuilds(this.profile.dir, options.approvedBuilds);
@@ -904,13 +910,28 @@ let PluginManager = (() => {
 				}
 				const files = await this.readRestoredFiles();
 				const before = readProfileManifest("dsh", this.profile.dir).dependencies ?? {};
+				const backend = this.ownerContext.get("profileBundlePackageBackend");
 				announce("installing");
 				let name;
+				let mutation;
 				try {
-					result.packageResult = await this.runPnpm(["add", spec], control.abort.signal, requestId);
+					if (backend === void 0) {
+						result.packageResult = await this.runPnpm(["add", parsed.spec], control.abort.signal, requestId);
+					} else {
+						mutation = await backend.install({
+							spec: parsed.spec,
+							kind: parsed.kind,
+							path: parsed.path,
+							expectedName: parsed.name,
+							signal: control.abort.signal,
+							onOutput: (text, stream) => this.emitPackageOutput(requestId, text, stream, ["pnpm", "add", parsed.spec])
+						});
+						result.packageResult = mutation.packageResult;
+					}
 					if (stopped()) throw new InstallCancelledError();
 					if (result.packageResult.exitCode !== 0) {
-						try {
+						if (mutation?.pendingBuilds !== void 0) result.pendingBuilds = mutation.pendingBuilds;
+						else try {
 							result.pendingBuilds = await readPendingBuilds(this.profile.dir);
 						} catch (error) {
 							this.ownerContext.logger.warn("Could not read pending build approvals after pnpm failed", error);
@@ -918,8 +939,8 @@ let PluginManager = (() => {
 						throw new Error(result.packageResult.output);
 					}
 					const after = readProfileManifest("dsh", this.profile.dir).dependencies ?? {};
-					const installed = Object.keys(after).filter((name) => before[name] !== after[name]);
-					if (installed.length === 0) installed.push(...Object.keys(after).filter((name) => spec === name || spec.startsWith(`${name}@`)));
+					const installed = mutation?.bundle === void 0 ? Object.keys(after).filter((name) => before[name] !== after[name]) : [mutation.bundle];
+					if (installed.length === 0) installed.push(...Object.keys(after).filter((name) => parsed.spec === name || parsed.spec.startsWith(`${name}@`)));
 					const target = installed[0];
 					if (installed.length !== 1 || target === void 0) throw new ManagementFailure("ambiguous-install");
 					name = target;
@@ -928,17 +949,19 @@ let PluginManager = (() => {
 					if (manifest?.dsh?.bundle?.patch === void 0) throw new ManagementFailure("not-bundle");
 					loadOverlayPatches("dsh", join(dir, manifest.dsh.bundle.patch));
 				} catch (error) {
+					await mutation?.rollback?.();
 					await this.restoreFiles(files);
 					throw error;
 				}
 				control.phase = "applying";
 				announce("applying");
+				mutation?.commit?.();
 				result.bundle = name;
 				result.target = name;
 				result.stage = "enable";
 				return this.configure(async () => {
 					if (options?.enabled !== false) await this.selectBundle(name, true);
-					if (Object.hasOwn(before, name)) return "restart-required";
+					if (mutation?.replaced === true || Object.hasOwn(before, name)) return "restart-required";
 					if (options?.enabled !== false) result.warnings = await this.reload();
 				});
 			}, {
@@ -986,7 +1009,13 @@ let PluginManager = (() => {
 					}
 					if ([...this.ctx.loader.entries()].some((entry) => entry.fiber?.uid != null && contributions.some((row) => row.id === entry.options.id && row.name === entry.options.name))) throw new ManagementFailure("bundle-in-use");
 				});
-				result.packageResult = await this.runPnpm(["remove", name]);
+				const backend = this.ownerContext.get("profileBundlePackageBackend");
+				const mutation = backend === void 0 ? void 0 : await backend.remove({
+					name,
+					signal: this.abort.signal,
+					onOutput: (text, stream) => this.emitPackageOutput(void 0, text, stream, ["pnpm", "remove", name])
+				});
+				result.packageResult = mutation?.packageResult ?? await this.runPnpm(["remove", name]);
 				if (result.packageResult.exitCode !== 0) throw new Error(result.packageResult.output);
 			}, {
 				stage: "remove",
@@ -1022,6 +1051,17 @@ let PluginManager = (() => {
 				overrides: [...new Set(patches.flatMap((item) => item.insert === void 0 && typeof item.id === "string" && !declared.has(item.id) ? [item.id] : []))]
 			};
 		}
+		/** Emit package backend output with the same Remote event contract as pnpm. */
+		emitPackageOutput(requestId, text, stream, argv) {
+			this.ownerContext.emit("plugin-manager/install-log", {
+				...requestId === void 0 ? {} : { requestId },
+				jobId: randomUUID(),
+				argv,
+				cwd: this.profile.dir,
+				stream,
+				text
+			});
+		}
 		/** Run one pnpm command in the profile, streaming its output as install-log chunks. */
 		async runPnpm(args, signal, requestId) {
 			const jobId = randomUUID();
diff --git a/node_modules/@deepseek-ai/dsh-plugin-manager/lib/types/index.d.ts b/node_modules/@deepseek-ai/dsh-plugin-manager/lib/types/index.d.ts
index f752ed8..b653f40 100644
--- a/node_modules/@deepseek-ai/dsh-plugin-manager/lib/types/index.d.ts
+++ b/node_modules/@deepseek-ai/dsh-plugin-manager/lib/types/index.d.ts
@@ -6,6 +6,12 @@ export type * from './types.ts';
 export { classifyInstallFailure, type InstallFailureFacts } from './install-failure.ts';
 export { InvalidInstallSpecError, parseInstallSpec, type ParsedInstallSpec } from './install-spec.ts';
 /** The pnpm executable and the limits for package diagnostics and registry lookups. */
+/** Optional host-owned package mutation backend. Hosts with immutable package
+ * generations install and remove through this seam; ordinary profiles retain pnpm. */
+export interface ProfileBundlePackageBackend {
+    install(request: { spec: string; kind: 'registry' | 'path' | 'git' | 'tarball'; path?: string; expectedName?: string; signal: AbortSignal; onOutput(text: string, stream: 'stdout' | 'stderr'): void; }): Promise<{ packageResult: import('./types.ts').PackageResult; bundle?: string; replaced?: boolean; pendingBuilds?: string[]; rollback?(): Promise<void>; commit?(): void; }>;
+    remove(request: { name: string; signal: AbortSignal; onOutput(text: string, stream: 'stdout' | 'stderr'): void; }): Promise<{ packageResult: import('./types.ts').PackageResult; }>;
+}
 export interface Config {
     /** The pnpm executable name or path; resolved through `PATH` like the `dsh plugin` command. */
     pnpmCommand?: string;
