diff --git a/node_modules/@deepseek-ai/dsh-agent/lib/index.js b/node_modules/@deepseek-ai/dsh-agent/lib/index.js
index 775dd62..1d0a64a 100644
--- a/node_modules/@deepseek-ai/dsh-agent/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-agent/lib/index.js
@@ -93,23 +93,32 @@ function foldConsumedWork(events) {
 function sameRoute(left, right) {
 	return left.provider === right.provider && left.model === right.model;
 }
-function routeLabel(route, other) {
-	return route.provider === other.provider ? route.model : `${route.provider}/${route.model}`;
-}
-function modelSwitchNotice(previous, selected) {
-	const from = routeLabel(previous, selected);
-	const to = routeLabel(selected, previous);
+async function modelSwitchNotice(ctx, previous, selected, signal) {
+	// A retired route can lose its catalog entry; labels are optional metadata.
+	const routes = [previous, selected];
+	const resolved = await Promise.allSettled(routes.map((route) =>
+		ctx.llm.resolveModelInfo(route.provider, route.model, signal)));
+	const providers = ctx.llm.listProviders();
+	const labels = routes.map((route, index) => {
+		const result = resolved[index];
+		const name = result.status === 'fulfilled' ? result.value.name.trim() || route.model : route.model;
+		const provider = providers.find((entry) => entry.id === route.provider)?.name || route.provider;
+		return previous.provider === selected.provider ? name : `${provider} / ${name}`;
+	});
+	const [from, to] = labels[0] === labels[1]
+		? labels.map((label, index) => `${label} (${routes[index].model})`)
+		: labels;
 	return createUserMessage({
 		content: [{
-			type: "text",
-			text: `[model changed: assistant turns above this point were generated by ${from}; the session continues with ${to}]`
+			type: 'text',
+			text: `[model changed: assistant turns above this point were generated by ${from}; the session continues with ${to}]`,
 		}],
 		source: {
-			kind: "plugin",
-			plugin: "model-selection",
-			form: "notice",
-			summary: boundContextSummary(`${from} → ${to}`)
-		}
+			kind: 'plugin',
+			plugin: 'model-selection',
+			form: 'notice',
+			summary: boundContextSummary(`${from} → ${to}`),
+		},
 	});
 }
 /**
@@ -164,10 +173,9 @@ function installModelSelection(agentCtx, selection) {
 		const selected = selection.assembled;
 		const previous = agent.session.requestHeader()?.config;
 		if (selected === void 0 || previous === void 0 || sameRoute(selected, previous)) return decision;
-		return {
-			...decision,
-			messages: [...decision.messages, modelSwitchNotice(previous, selected)]
-		};
+		const notice = await modelSwitchNotice(agentCtx, previous, selected, signal);
+		if (signal.aborted) return decision;
+		return { ...decision, messages: [...decision.messages, notice] };
 	}, { prepend: true });
 	return () => {
 		disposeAssembly();
diff --git a/node_modules/@deepseek-ai/dsh-agent/lib/types/model-selection.js b/node_modules/@deepseek-ai/dsh-agent/lib/types/model-selection.js
index 1997113..970eb9c 100644
--- a/node_modules/@deepseek-ai/dsh-agent/lib/types/model-selection.js
+++ b/node_modules/@deepseek-ai/dsh-agent/lib/types/model-selection.js
@@ -6,17 +6,26 @@ import { boundContextSummary, createUserMessage, } from '@deepseek-ai/dsh-llm';
 function sameRoute(left, right) {
     return left.provider === right.provider && left.model === right.model;
 }
-function routeLabel(route, other) {
-    return route.provider === other.provider ? route.model : `${route.provider}/${route.model}`;
-}
-function modelSwitchNotice(previous, selected) {
-    const from = routeLabel(previous, selected);
-    const to = routeLabel(selected, previous);
+async function modelSwitchNotice(ctx, previous, selected, signal) {
+    // A retired route can lose its catalog entry; labels are optional metadata.
+    const routes = [previous, selected];
+    const resolved = await Promise.allSettled(routes.map((route) =>
+        ctx.llm.resolveModelInfo(route.provider, route.model, signal)));
+    const providers = ctx.llm.listProviders();
+    const labels = routes.map((route, index) => {
+        const result = resolved[index];
+        const name = result.status === 'fulfilled' ? result.value.name.trim() || route.model : route.model;
+        const provider = providers.find((entry) => entry.id === route.provider)?.name || route.provider;
+        return previous.provider === selected.provider ? name : `${provider} / ${name}`;
+    });
+    const [from, to] = labels[0] === labels[1]
+        ? labels.map((label, index) => `${label} (${routes[index].model})`)
+        : labels;
     return createUserMessage({
         content: [{
-                type: 'text',
-                text: `[model changed: assistant turns above this point were generated by ${from}; the session continues with ${to}]`,
-            }],
+            type: 'text',
+            text: `[model changed: assistant turns above this point were generated by ${from}; the session continues with ${to}]`,
+        }],
         source: {
             kind: 'plugin',
             plugin: 'model-selection',
@@ -85,7 +94,9 @@ export function installModelSelection(agentCtx, selection) {
         const previous = agent.session.requestHeader()?.config;
         if (selected === undefined || previous === undefined || sameRoute(selected, previous))
             return decision;
-        return { ...decision, messages: [...decision.messages, modelSwitchNotice(previous, selected)] };
+        const notice = await modelSwitchNotice(agentCtx, previous, selected, signal);
+        if (signal.aborted) return decision;
+        return { ...decision, messages: [...decision.messages, notice] };
     }, { prepend: true });
     return () => {
         disposeAssembly();
