diff --git a/node_modules/@deepseek-ai/dsh-agent-default-model/lib/index.js b/node_modules/@deepseek-ai/dsh-agent-default-model/lib/index.js
index f84a9a1..3e33ded 100644
--- a/node_modules/@deepseek-ai/dsh-agent-default-model/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-agent-default-model/lib/index.js
@@ -13,7 +13,12 @@ const AGENT_DEFAULT_MODEL_SETTINGS_NAMESPACE = "agent-default-model";
 const AGENT_DEFAULT_MODEL_SETTINGS_SCHEMA = z.object({
 	provider: z.string().required(),
 	model: z.string().required(),
-	reasoningEffort: z.string()
+	reasoningEffort: z.string(),
+	previousProviderSelection: z.object({
+		provider: z.string().required(),
+		model: z.string().required(),
+		reasoningEffort: z.string()
+	}).default(void 0)
 });
 /** Project stored settings onto the Agent-facing selection type. */
 function selection(settings) {
@@ -34,12 +39,14 @@ var AgentDefaultModelConfig = class extends Service {
 		model: z.string().required()
 	});
 	source;
+	entry;
 	constructor(ctx, config) {
 		super(ctx, "agentDefaultModel");
 		const entry = {
 			provider: config.provider,
 			model: config.model
 		};
+		this.entry = entry;
 		this.source = () => entry;
 		ctx.inject(["settings"], (settingsCtx) => {
 			settingsCtx.settings.installSection(ctx, AGENT_DEFAULT_MODEL_SETTINGS_NAMESPACE, AGENT_DEFAULT_MODEL_SETTINGS_SCHEMA, entry, {
@@ -63,10 +70,18 @@ var AgentDefaultModelConfig = class extends Service {
 	* @param next - resolved selection accepted by an entry point.
 	* @returns fulfillment after the optional settings write settles.
 	*/
+	fallbackSelection(excludedProvider) {
+		const stored = this.source();
+		const candidate = [stored, stored.previousProviderSelection, this.entry].find(value => value && value.provider !== excludedProvider);
+		return candidate === void 0 ? void 0 : selection(candidate);
+	}
 	async saveSelection(next) {
+		const stored = this.source();
+		const previous = stored.provider === next.provider ? stored.previousProviderSelection : selection(stored);
 		await this.ctx.get("settings")?.replace(AGENT_DEFAULT_MODEL_SETTINGS_NAMESPACE, {
 			provider: next.provider,
 			model: next.model,
+			...previous === void 0 ? {} : { previousProviderSelection: previous },
 			...next.reasoningEffort === void 0 ? {} : { reasoningEffort: String(next.reasoningEffort) }
 		});
 	}
diff --git a/node_modules/@deepseek-ai/dsh-agent-default-model/lib/types/index.d.ts b/node_modules/@deepseek-ai/dsh-agent-default-model/lib/types/index.d.ts
index 3239237..9d3781f 100644
--- a/node_modules/@deepseek-ai/dsh-agent-default-model/lib/types/index.d.ts
+++ b/node_modules/@deepseek-ai/dsh-agent-default-model/lib/types/index.d.ts
@@ -22,6 +22,8 @@ export interface AgentDefaultModelSettings {
     model: string;
     /** Adapter-owned reasoning effort, or provider/default behavior when absent. */
     reasoningEffort?: string;
+    /** Last selection from a different provider, retained across model changes. */
+    previousProviderSelection?: ModelSelection;
 }
 /** Schema of the default Agent model settings section. */
 export declare const AGENT_DEFAULT_MODEL_SETTINGS_SCHEMA: z<AgentDefaultModelSettings>;
@@ -40,6 +42,7 @@ export interface Config {
 export declare class AgentDefaultModelConfig extends Service {
     static Config: z<Config>;
     private source;
+    private entry;
     constructor(ctx: Context, config: Config);
     /**
      * Read the current default model selection.
@@ -52,6 +55,7 @@ export declare class AgentDefaultModelConfig extends Service {
      * @param next - resolved selection accepted by an entry point.
      * @returns fulfillment after the optional settings write settles.
      */
+    fallbackSelection(excludedProvider: string): ModelSelection | undefined;
     saveSelection(next: ModelSelection): Promise<void>;
 }
 export default AgentDefaultModelConfig;
