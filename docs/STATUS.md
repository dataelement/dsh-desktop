# 企业接入交付状态

更新：2026-09-20。

本分支面向 `feat/bisheng-gateway-auth@9b2d2cf17e8f25f3c3cc1af297a25760cb0731f0`，与当次核验的 `V0.9.1` 基线一致。Harness 声明及安装版本为 `0.1.5-rc.2`。

## 已实现

- 企业登录、主进程凭据保管、PKCE 回调、模型代理和授权失效后的可用模型恢复，接续 PR #457 的企业实现 `a4cc4d25`（原 PR #422 的后续迁移）。
- 企业模型的图片能力、模型目录与用量同步，以及跟随产品语言的账号页和错误提示。
- 视觉模型标识、账号页模型卡片和中文 Token 万单位展示，接续 PR #480 的 `568225ff`。
- `feat/bisheng-gateway-auth` 的 PR 使用现有 Windows 自动验证和开发安装包工作流。

## 自动验证

- 全量测试：137 个文件、1,157 项通过。
- TypeScript 类型检查、生产构建、diff 检查和工作流目标分支配置检查通过。
- package.json、锁文件和全部依赖差异文件与已验证的 #480 工作树一致；复用对应依赖副本并在当前工作树重跑上述检查。
- 首轮受限环境的回环端口测试遇到沙箱约束；允许本地回环服务的完整重跑通过。

## 实际环境验收

真实企业登录、原生 Desktop 唤起、系统钥匙串、真实模型图片问答及新安装包验收：`NOT_RUN`。上述自动验证覆盖代码行为和构建结果。
