# 原始工作台与规范差异检查（待确认）

状态：两个已适配工作台已从当前 Desktop 开发版卸载。保留会话、业务资料及旧源码。以下只读检查基于重新从 GitHub 克隆的原版，未修改原版代码，也未重新安装。拟修改项须经用户确认后执行。

## 原始版本

- 玄学：`3666033d13e0ed84f5b6467a4c5e035da370ce35`，目录 `/Users/cinder/Desktop/Coding/DSHCoding/dsh-ming-life-original-review-20260914`。
- 选址：`7bf06a1d8dc5873425988ae7d189a93303a17b36`，目录 `/Users/cinder/Desktop/Coding/DSHCoding/dsh-site-selection-original-review-20260914`。

## 应当保留的原有行为

两个工作台均为：创建业务资料 → 用业务资料目录自动创建工作区和会话 → 打开会话 → 保存关联 → 填入开场提示词草稿。已有业务资料恢复保存的会话，必要时新建。用户不需要先手工创建会话。

证据：玄学 `lib/client.js:104,154,171,305`；选址 `lib/client.js:137,216,249,484`。

“业务资料与 DSH 工作区是不同概念”不等于禁止自动关联。原版用业务资料目录创建工作区，无需改成让用户另选目录。

原有业务表单、地图、档案选择、数据计算及业务提示词应保留。开场提示词只填草稿，用户回车发送；玄学另有自动解读流程（`src/components/Interpretation.jsx:26`、`src/App.jsx:113`、`lib/client.js:261`），不能混为一谈，也不能在适配时顺带删除。

## 拟修改范围

| 项目 | 原版与规范的差异 | 拟修改方式 |
| --- | --- | --- |
| 市场登记 | 缺少工作台身份和兼容声明 | 补充标识、版本、兼容信息及注册 |
| 工作台呈现与切换 | 各自注册导航、shell.overlay 并修改外壳布局 | 改为 Desktop 控制挂载和切换；保留业务面板内部设计，不注入统一工具栏 |
| 会话归属 | 原版创建或恢复会话，但未登记工作台归属 | 保留原创建触发点和资料目录，通过宿主接口统一记录归属和处理切换竞争 |
| 无会话使用 | openProject 等待 ensureSession 成功后才显示 | 业务面板立即可用，自动建会话并行进行；失败可重试，不阻断业务面板 |
| 消息与历史恢复 | 消息桥主要校验 origin，缺少 source 和工作台归属约束 | 保留历史映射恢复，补充 iframe 来源和当前所属会话检查，避免隐藏工作台误写其他会话 |
| 删除边界 | 选址会清理业务目录中已消失项目对应的 native workspace | 检查共享工作区和旧会话引用，避免误清理；切换和卸载不清理业务资料 |

## 规范需要纠正的表述

中文规范并未要求禁止业务操作自动创建会话。此前适配文档中的“工作台不创建、恢复会话”限制应改为：工作台可通过 Desktop 接口触发创建或恢复，宿主统一校验与记录归属。

“无会话时暂存提示词”只是创建失败或延迟时的兜底，不能替代这两个工作台原有的自动建会话流程。

## 修改后的验收主线（待批准后执行）

1. 没有已有会话和工作区时，打开工作台即可看到业务面板。
2. 创建档案或选址项目后，自动创建对应工作区、会话并填入开场草稿。
3. 重新打开业务资料，恢复其原有会话；切换会话恢复对应业务资料。
4. 创建失败时资料仍可用；切换过程中不会误绑定或抢回前台。
5. 原有自动解读和业务功能保留，并在正确所属会话执行。
6. 切换或卸载不删除会话、业务资料或共享工作区。
