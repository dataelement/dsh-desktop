// The Profile package and Desktop's in-box entry share this package name.
// Safe Mode treats only the installed Profile package as a disable target.
export const IMAGE_GENERATION_PLUGIN = 'dsh-image-generation'
